version: '2'
services:
  learndb:
    image: mysql:5.7
    volumes:
      - learn_db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: somewordpresslearn
      MYSQL_DATABASE: wordpresslearn
      MYSQL_USER: wordpresslearn
      MYSQL_PASSWORD: wordpresslearn
    networks:
      - ILICEReverseProxy

  learn:
    depends_on:
      - learndb
    image: wordpress:latest
    container_name: learn
    restart: always
    expose:
      - '80'
    ports:
      - '8001:80'
    volumes:
      - learn_wp_folder:/var/www/html
      - ./.htaccess:/var/www/html/.htaccess
    environment:
     WORDPRESS_DB_HOST: learndb:3306
     WORDPRESS_DB_USER: wordpresslearn
     WORDPRESS_DB_PASSWORD: wordpresslearn
    networks:
     - ILICEReverseProxy

volumes:
   learn_db_data:
   learn_wp_folder:
   aula_learn_db_data:
   aula_learn_wp_folder:

networks:
  ILICEReverseProxy:
    external: true
